name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      # Pages serves every file with the same short max-age and no fingerprint
      # in its URL, so a browser can end up pairing a cached page with scripts
      # from another deploy. Stamping both sides with the commit lets the page
      # notice and reload itself (public/js/version.js).
      - name: Stamp the build version
        run: node scripts/stamp-version.mjs "${GITHUB_SHA::12}"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
